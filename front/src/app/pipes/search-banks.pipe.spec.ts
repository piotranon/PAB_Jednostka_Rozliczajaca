import { SearchBanksPipe } from './search-banks.pipe';

describe('SearchBanksPipe', () => {
  it('create an instance', () => {
    const pipe = new SearchBanksPipe();
    expect(pipe).toBeTruthy();
  });
});
