import { SearchTransactionPipe } from './search-transaction.pipe';

describe('SearchTransactionPipe', () => {
  it('create an instance', () => {
    const pipe = new SearchTransactionPipe();
    expect(pipe).toBeTruthy();
  });
});
